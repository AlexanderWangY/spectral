// Parse config here (for initial testing, should be removed later)
// The config should be a TOML file
// This will be removed later in favor of the downwards API in kube
// this is because real world application will inject their info at runtime.
