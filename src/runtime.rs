pub struct Runtime {}

impl Runtime {
    pub fn start() -> Self {
        // Initialize mspc channels

        // Construct actors

        // Inject channels

        // Kick off run

        Self {}
    }
}
